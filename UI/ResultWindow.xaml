<Window x:Class="UI_DataUpload.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI_DataUpload"
        mc:Ignorable="d"
        Title="ResultWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Name="FileNameTextBox" Margin="5" IsReadOnly="true" />
        <ListBox Grid.Row="1" Name="ResultsListBox" Margin="5" />
        <Button Grid.Row="2" Name="UploadButton" Margin="5" Content="Upload" Click="UploadButton_Click"/>

        <Button Grid.Row="3" Name="CloseButton" Margin="5" Content="Close" Click="CloseButton_Click"/>
    </Grid>
</Window>
